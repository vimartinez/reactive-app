CREATE TABLE IF NOT EXISTS car (
                                   id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                   brand VARCHAR(255),
    model VARCHAR(255),
    registration VARCHAR(255),
    yearr INT
    );